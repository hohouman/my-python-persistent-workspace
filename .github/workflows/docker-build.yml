name: Build and Push to GitHub Packages

on:
  push:
    branches:
      - "main"
    # 也可以手动触发构建
    workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # 自动获取当前仓库名称 (格式: 用户名/仓库名)
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    # !重要!：赋予 GITHUB_TOKEN 写入包的权限
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 登录到 GitHub Container Registry
      # 使用系统自动生成的 GITHUB_TOKEN，无需你去设置 Secrets
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 自动处理 Docker 标签和元数据 (会自动将仓库名转换为小写，防止构建报错)
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,format=long

      # 确保 QEMU 设置步骤存在 (这对 ARM 构建至关重要)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # 关键修改：同时构建 amd64 (普通PC) 和 arm64 (你的服务器)
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # 必须传递 build-args 才能让 Dockerfile 里的 ARG 生效吗？
          # 其实 Buildx 会自动注入 TARGETARCH，但显式声明也没坏处
